# Agent Brief

Generated by agent-rake for `rake`.

## Repo Snapshot

- Root: `rake`
- Files scanned: 24
- Primary languages: JavaScript (6)
- Package manager: npm

## Stack Signals

- Manifests: `package.json`
- Lockfiles: `package-lock.json`
- Framework signals: none detected
- Workspace signals: none detected
- Docs: `CHANGELOG.md`, `CONTRIBUTING.md`, `LICENSE`, `README.md`, `RELEASE.md`, `SECURITY.md`, `examples/README.md`
- Existing agent instructions: `AGENTS.md`

## Commands Agents Should Prefer

- `npm start` - package.json script: node ./bin/agent-rake.js
- `npm test` - package.json script: node --test
- `npm run brief` - package.json script: node ./bin/agent-rake.js
- `npm run brief:check` - package.json script: node ./bin/agent-rake.js --check
- `npm run pack:check` - package.json script: npm pack --dry-run --cache .npm-cache
- `npm run release:check` - package.json script: npm test && npm run smoke && npm run smoke:modes && npm run brief:check && npm run pack:check
- `npm run smoke` - package.json script: node ./bin/agent-rake.js --dry-run --root .
- `npm run smoke:modes` - package.json script: node ./bin/agent-rake.js --summary --strict --ignore .npm-cache --root . && node ./bin/agent-rake.js --doctor --strict --root . && node ./bin/agent-rake.js --targets && node ./bin/agent-rake.js --completion bash && node ./bin/agent-rake.js --completion powershell && node ./bin/agent-rake.js --github-actions && node ./bin/agent-rake.js --config-example && node ./bin/agent-rake.js --show-config --root . && node ./bin/agent-rake.js --diff --root .

## Files To Read First

- Entrypoints: `src/index.js`
- Routes and APIs: none detected
- Tests: `test/index.test.js`
- Environment files: none detected
- Data and schema files: none detected
- CI: `.github/workflows/ci.yml`
- Config: none detected

## Risk Hotspots

- No obvious auth, payment, migration, webhook, database, queue, or security hotspot files were detected.

## Agent Rules For This Repo

- Start with the files listed above before making broad edits.
- Preserve generated, dependency, cache, build, and vendor directories.
- Treat `.env` and secret-bearing files as sensitive; prefer examples and docs.
- Prefer the detected package manager and commands over global fallbacks.
- Run the smallest relevant test command before claiming a code change is done.
